{
  "name": "getraenke-app",
  "version": "0.1.0",
  "private": true,
  "description": "Bergwacht Zollernalb — Getränkekasse (Monorepo)",
  "scripts": {
    "dev": "concurrently -n backend,frontend,game -c blue,magenta,green \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\" \"pnpm --filter game dev\"",
    "build": "pnpm --filter backend build && pnpm --filter frontend build && pnpm --filter game build",
    "test": "pnpm --filter backend test",
    "seed": "pnpm --filter backend seed",
    "db:push": "pnpm --filter backend db:push",
    "db:reset": "pnpm --filter backend db:reset"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.1.3"
}
